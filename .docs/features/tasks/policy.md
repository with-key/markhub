# 태스크 정책서

## 1. 상태 정책

- 태스크 상태는 `backlog`, `todo`, `in progress`, `done`만 허용한다.
- 상태값은 자유 입력이 아닌 제한된 값으로 관리한다.

## 2. 소속 정책

- 태스크는 반드시 하나의 프로젝트에 속한다.
- 소속 에픽은 선택값이지만, 선택 시 같은 프로젝트 에픽이어야 한다.

## 3. 삭제 정책

- MVP에서는 태스크 삭제를 기본 범위에 포함하지 않는다.
